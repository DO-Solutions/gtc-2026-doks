apiVersion: v1
kind: Service
metadata:
  name: loadgen
  namespace: dynamo-workload
  labels:
    app: loadgen
spec:
  type: ClusterIP
  selector:
    app: loadgen
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
